apiVersion: apps/v1
kind: Deployment
metadata:
  name: flappy-ro
  namespace: flappy-ro
spec:
  replicas: 2
  selector:
    matchLabels:
      app: flappy-ro
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: flappy-ro
    spec:
      containers:
        - name: flappy-ro
          image: mariuseu/flappy-ro:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: flappy-secret
                  key: DATABASE_URL
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: flappy-ro-service
  namespace: flappy-ro
spec:
  type: NodePort
  selector:
    app: flappy-ro
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 32527
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: flappy-ro-ingress
  namespace: flappy-ro
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
    - host: flappy.slax.ro
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: flappy-ro-service
                port:
                  number: 8000
